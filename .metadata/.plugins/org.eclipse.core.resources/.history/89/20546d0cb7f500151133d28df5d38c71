
public abstract class Hand extends CardList {
	
	private CardGamePlayer player;
	private CardList cardListNotVerify;
	
	public Hand( CardGamePlayer player, CardList cards){
		this.cardListNotVerify=cards;
		this.player = player;
	}
	
	public CardGamePlayer getPlayer(){
		return this.player;
	}
	
	public Card getTopCard(){
		this.cardListNotVerify.sort();
		return cardListNotVerify.getCard(cardListNotVerify.size());
	}
	public boolean beats(Hand hand){
		this.cardListNotVerify.sort();
		CardList sorted = this.cardListNotVerify;
		int largestCardIndex = cardListNotVerify.size()-1;
		if(sorted.getCard())
	}
	abstract boolean isValid();																		
	abstract String getType(); 
}
